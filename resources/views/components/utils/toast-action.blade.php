<button 
    type="button" 
    class="btn btn-primary toast-trigger" 
    data-toast="toast-content-action" 
    data-title="{{ $title }}" 
    data-message="{{ $message }}"
    data-id="{{ $id }} "
    data-action="{{ $action }} "
    data-type="{{ $type }}"
    data-method="{{ $method }}"
>
    {{ $title }}
</button>